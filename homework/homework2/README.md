# Project Title
**Stage:** Tooling Setup (Stage 02)

# Prpject Purpose
To set up .env and create a config.py.  
Then, create a jupyter check by running a small array operation, and check freeze dependencies.           